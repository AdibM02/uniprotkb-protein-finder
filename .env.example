# UniProtKB Protein Finder - Environment Configuration
#
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# The .env file is ignored by git (see .gitignore) to protect sensitive data.
# Only commit .env.example with example values.

# Required: Your email address for UniProtKB API requests
# The API uses this to monitor usage and contact you if needed
# See: https://www.uniprot.org/help/api
CONTACT_EMAIL=your.email@example.com

# Optional: Directory for exporting protein data
# Default: ./output (relative to application directory)
# Will be auto-created if it doesn't exist
OUTPUT_DIR=./output


# Output directory for JSON files (defaults to ./output if not set)
# OUTPUT_DIR=./output
